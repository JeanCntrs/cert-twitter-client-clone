const initialState = {
    names: '',
    surnames: ''
}

export default function (state = initialState, action) {
    switch (action.type) {
        // All case

        default:
            return state;
    }
}